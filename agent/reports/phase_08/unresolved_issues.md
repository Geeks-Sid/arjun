# Unresolved issues

- The existing license records keep H-Optimus, GigaPath-Flash, CONCH, and TITAN upstream weights blocked or pending review. Phase 08 therefore verifies the adapter boundaries and offline tiny path only; it does not claim upstream checkpoint execution.
- CUDA and TPU hardware runs were not available in this environment. The code exposes fixed tile/token buckets and deterministic rank sharding; host-side OpenSlide/TiffSlide decoding and optional cuCIM policy remain explicit.
- Throughput and peak host/device memory for real gigapixel slides require a licensed checkpoint and representative hardware; no fabricated benchmark is recorded.
